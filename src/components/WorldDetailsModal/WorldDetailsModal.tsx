type WorldDetailsProps = {
    worldShortDesc: string;
    worldID: string;
}

export default function WorldDetailsModal(props: WorldDetailsProps) {
    return (
        <div className="absolute top-0 left-0 h-screen w-screen bg-lightblue">
        </div>
    );
}